<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="create_journal_entry_from_house_costs" model="ir.actions.server">
        <field name="name">Create Expense Entry</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="freight_management_charges.model_house_shipment_charge_cost"/>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_create_expense_entry()
        </field>
        <field name="binding_model_id" ref="freight_management_charges.model_house_shipment_charge_cost"/>
        <field name="binding_view_types">list</field>
    </record>
    <record id="create_journal_entry_from_master_costs" model="ir.actions.server">
        <field name="name">Create Expense Entry</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="freight_management_charges.model_master_shipment_charge_cost"/>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_create_expense_entry()
        </field>
        <field name="binding_model_id" ref="freight_management_charges.model_master_shipment_charge_cost"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
