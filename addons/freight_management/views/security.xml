<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="group_allow_status_change_of_completed_shipment" model="res.groups">
            <field name="name">Completed/Cancelled Shipment Status Change</field>
            <field name="implied_ids" eval="[(4, ref('base.user_admin'))]"/>
            <field name="comment">The user can change the status of completed or cancelled house shipment and master shipment.</field>
            <field name="users" eval="[(4, ref('base.user_admin'))]"/>
        </record>
    </data>
</odoo>