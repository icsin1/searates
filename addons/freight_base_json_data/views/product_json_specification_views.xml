<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_json_specification_view_search" model="ir.ui.view">
        <field name="name">product.json.specification.view.search</field>
        <field name="model">product.json.specification</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="model_id" />
                <filter name="filter_main_only" string="Main Spec" domain="[('linked_property_ids', '=', False)]" />
            </search>
        </field>
    </record>

    <record id="product_json_specification_view_tree" model="ir.ui.view">
        <field name="name">product.json.specification.view.tree</field>
        <field name="model">product.json.specification</field>
        <field name="arch" type="xml">
            <tree>
                <field name="display_name" />
                <field name="model_id"/>
            </tree>
        </field>
    </record>

    <record id="product_json_specification_property_view_form" model="ir.ui.view">
        <field name="name">product.json.specification.property.view.form</field>
        <field name="model">product.json.specification.property</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <label for="property_type"/>
                    <div class="oe_title">
                        <h3><field name="property_type" widget="radio" options="{'horizontal': True}" /></h3>
                        <h2 class="o_row">
                            <field name="ir_model_field" placeholder="Model Field" attrs="{'invisible': [('property_type', '!=', 'field')], 'required': [('property_type', '=', 'field')]}" options="{'no_create': 1}"/>
                            <field name="name" placeholder="Property Key"/>
                        </h2>
                    </div>
                    <group>
                        <group>
                            <field name="model_id" invisible="1"/>
                            <field name="product_domain" invisible="1"/>
                            <field name="relation_model_id" attrs="{'invisible': [('ttype', 'not in', ['many2one', 'many2many', 'one2many'])], 'required': [('ttype', 'in', ['many2one', 'many2many', 'one2many'])]}"/>
                            <field name="ttype" />
                            <field name="is_required" widget="boolean_toggle"/>
                        </group>
                        <group>
                            <field name="noupdate" widget="boolean_toggle"/>
                            <field name="raw_value" widget="boolean_toggle" attrs="{'invisible': [('ttype', 'not in', ['date', 'datetime', 'selection', 'monetary'])]}"/>
                            <field name="property_specification_id" attrs="{'invisible': [('ttype', 'not in', ['many2one', 'many2many', 'one2many'])]}"
                                    context="{'default_model_id': relation_model_id}" domain="[('model_id', '=', relation_model_id)]"/>
                            <label for="order_ir_field" attrs="{'invisible': [('ttype', 'not in', ['one2many', 'many2many'])]}"/>
                            <div class="o_row">
                                <field name="order_ir_field" attrs="{'invisible': [('ttype', 'not in', ['one2many', 'many2many'])]}"/>
                                <field name="order_field_type" widget="radio" options="{'horizontal': 1}" attrs="{'invisible': [('order_ir_field', '=', False)], 'required': [('order_ir_field', '!=', False)]}"/>
                            </div>
                            <field name="description" />
                        </group>
                    </group>
                    <notebook>
                        <page name="property_compute" string="Property Computation" attrs="{'invisible': [('property_type', '!=', 'prop')]}">
                            <field name="python_code" widget="ace" options="{'mode': 'python'}"/>
                        </page>
                        <page string="Help" name="help_info" attrs="{'invisible': [('property_type', '!=', 'prop')]}">
                            <group>
                                <div style="margin-top: 4px;">
                                    <h3>Help with Python expressions</h3>
                                    <p>Various fields may use Python code or Python expressions. The following variables can be used:</p>
                                    <ul>
                                        <li><code>env</code>: Environment on which the action is triggered</li>
                                        <li><code>model</code>: Model of the record on which the action is triggered; is a void recordset</li>
                                        <li><code>record</code>: record on which the action is triggered; may be be void</li>
                                        <li><code>records</code>: recordset of all records on which the action is triggered in multi mode; may be void</li>
                                        <li><code>time</code>, <code>datetime</code>, <code>dateutil</code>, <code>timezone</code>: useful Python libraries</li>
                                        <li><code>log(message, level='info')</code>:logging function to record debug information in <code>ir.logging</code> table</li>
                                        <li><code>UserError</code>: Warning Exception to use with <code>raise</code></li>
                                        <li>To return an result, assign: <code>result = {...}</code></li>
                                        <li>Note: For relation type of property x2m or m2o you need to return recordset</li>
                                    </ul>
                                </div>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="product_json_specification_view_form" model="ir.ui.view">
        <field name="name">product.json.specification.view.form</field>
        <field name="model">product.json.specification</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button string="Export Schema" name="action_export_spec_json" type="object" class="oe_highlight" groups="base.group_no_one"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_download_json_spec" type="object" class="oe_stat_button" icon="fa-cubes">
                            <div class="o_stat_info">
                                <span class="o_stat_value">
                                    Download
                                </span>
                                <span class="o_stat_text font-weight-bold">
                                    Json Spec
                                </span>
                            </div>
                        </button>
                    </div>
                    <label for="name"/>
                    <div class="oe_title">
                        <h1><field name="name" /></h1>
                    </div>
                    <group>
                        <group>
                            <field name="model_id" options="{'no_create': 1}"/>
                            <field name="model_name" invisible="1"/>
                            <field name="product_domain" widget="domain" options="{'model': 'model_name', 'in_dialog': True}"/>
                        </group>
                        <group>
                            <field name="empty_string_for_non_bool" widget="boolean_toggle"/>
                            <field name="null_for_empty" widget="boolean_toggle"/>
                            <field name="keep_structure" widget="boolean_toggle"/>
                            <field name="json_type" />
                            <field name="description" />
                        </group>
                    </group>
                    <notebook>
                        <page name="properties" string="Properties">
                            <field name="property_ids">
                                <tree>
                                    <field name="name" />
                                    <field name="property_type" />
                                    <field name="model_id" invisible="1"/>
                                    <field name="ttype" />
                                    <field name="is_required" />
                                    <field name="description" />
                                    <field name="noupdate" widget="boolean_toggle"/>
                                </tree>
                            </field>
                        </page>
                        <page name="dependency_property_ids" string="Dependency With Specification">
                            <field name="dependency_spec_ids" force_save="1" readonly="0">
                                <tree editable="bottom">
                                    <field name="name" string="Property" readonly="1" force_save="1"/>
                                    <field name="property_id"/>
                                    <field name="property_specification_id" readonly="1" force_save="1"/>
                                </tree>
                            </field>
                        </page>
                        <page name="linked_property_ids" string="Linked Properties">
                            <field name="linked_property_ids" widget="many2many" domain="[('relation_model_id', '=', model_id)]"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="product_json_speicfication_action" model="ir.actions.act_window">
        <field name="name">Product Json Specification</field>
        <field name="res_model">product.json.specification</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('json_type', '=', 'general')]</field>
        <field name="context">{'search_default_filter_main_only': 1, 'default_json_type': 'general'}</field>
    </record>

    <menuitem
        id="product_json_specification_menu"
        name="Product JSON Specifications"
        action="product_json_speicfication_action"
        parent="freight_base.root_freight_master_configuration"
        sequence="2"/>
</odoo>
