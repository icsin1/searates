<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="lead_source_form_view" model="ir.ui.view">
        <field name="name">lead.source.form.view</field>
        <field name="model">lead.source</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Source"/>
                        </h1>
                    </div>
                    <group>
                        <field name="allow_source_creation" widget="boolean_toggle"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="lead_source_tree_view" model="ir.ui.view">
        <field name="name">lead.source.tree.view</field>
        <field name="model">lead.source</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="lead_source_action" model="ir.actions.act_window">
        <field name="name">Lead Source</field>
        <field name="res_model">lead.source</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Lead Source.
            </p>
        </field>
    </record>

    <menuitem
        id="menu_lead_source"
        name="Lead Source"
        action="lead_source_action"
        parent="fm_sale_crm.fm_sales_configuration_menu"
        sequence="2"/>
</odoo>
