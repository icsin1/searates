<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="crm_prospect_lead_stage_view_tree" model="ir.ui.view">
        <field name="name">crm.prospect.lead.stage.view.tree</field>
        <field name="model">crm.prospect.lead.stage</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="is_create_opportunity"/>
            </tree>
        </field>
    </record>

    <record id="crm_prospect_lead_stage_view_form" model="ir.ui.view">
        <field name="name">crm.prospect.lead.stage.view.form</field>
        <field name="model">crm.prospect.lead.stage</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Negotiation"/>
                        </h1>
                    </div>
                    <group>
                        <field name="is_create_opportunity"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="crm_prospect_lead_stage_action" model="ir.actions.act_window">
        <field name="name">Lead Stage</field>
        <field name="res_model">crm.prospect.lead.stage</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new Prospect Lead Stage.
            </p>
        </field>
    </record>

    <menuitem
        id="crm_prospect_lead_stage_menu"
        name="Lead Stage"
        action="crm_prospect_lead_stage_action"
        parent="fm_sale_crm.fm_sales_configuration_menu"
        sequence="1"/>

</odoo>
