<?xml version="1.0" ?>

<odoo noupdate="1">
    <record model="ir.cron" id="ir_cron_generate_monthly_sales_target">
        <field name="name">Generate Monthly Sales Target</field>
        <field name="model_id" ref="fm_sale_crm.model_crm_sale_target"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="state">code</field>
        <field name="doall" eval="False" />
        <field name="code">model._cron_generate_monthly_target()</field>
    </record>
</odoo>
