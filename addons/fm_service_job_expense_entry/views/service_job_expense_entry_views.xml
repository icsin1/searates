<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_actions_server_create_journal_entry_from_costs" model="ir.actions.server">
        <field name="name">Create Expense Entry</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="fm_service_job_charges.model_service_job_charge_cost"/>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_create_expense_entry()
        </field>
        <field name="binding_model_id" ref="fm_service_job_charges.model_service_job_charge_cost"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
