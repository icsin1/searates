<?xml version="1.0" encoding="utf-8"?>
<odoo>
     <record id="action_open_settings" model="ir.actions.act_window">
        <field name="name">Settings</field>
        <field name="res_model">res.config.settings</field>
        <field name="view_mode">form</field>
        <field name="target">inline</field>
        <field name="context" eval="{'module': 'general_settings', 'bin_size': False}"/>
    </record>

    <record id="view_company_form" model="ir.ui.view">
        <field name="name">res.company.form.inherit.odoo_web</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[hasclass('oe_avatar')]" position="before">
                <div class="oe_button_box" name="button_box">
                    <field name="show_settings" invisible="1"/>
                    <button class="oe_stat_button"  name="%(action_open_settings)d" type="action" icon="fa-bars" attrs="{'invisible': [('show_settings', '=', False)]}">
                        Settings
                    </button>
                </div>
            </xpath>
            <xpath expr="//field[@name='favicon']" position="attributes">
                <attribute name="groups" modify="remove"/>
            </xpath>
        </field>
    </record>

</odoo>