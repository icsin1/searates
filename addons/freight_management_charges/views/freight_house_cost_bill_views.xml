<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_actions_server_create_vendor_bill_from_costs" model="ir.actions.server">
        <field name="name">Create Vendor Bill</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="model_house_shipment_charge_cost"/>
        <field name="state">code</field>
        <field name="code">
            if records:
                action = records.action_create_vendor_bill()
        </field>
        <field name="binding_model_id" ref="model_house_shipment_charge_cost"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
